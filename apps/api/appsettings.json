{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Identity": {
    "//": "Fallback resolver for X-Api-Key headers that don't match a local api_key row. Points at the mt-oidc service (auth.mettel.exercise.dany.codes in prod). Override with the BRUIN_IDENTITY_URL env var, or clear it to disable the fallback.",
    "BaseUrl": "https://auth.mettel.exercise.dany.codes"
  }
}
